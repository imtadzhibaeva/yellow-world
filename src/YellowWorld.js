import React from 'react';

const YellowWorld = () => {
  const style = {
    backgroundColor: 'yellow',
    height: '100vh',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    fontSize: '2rem',
  };

  return (
    <div style={style}>
      Yellow World
    </div>
  );
};

export default YellowWorld;
